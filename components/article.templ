package components

import "github.com/cleanupDev/blog/internal"

templ ArticlePage(article internal.Article) {
	@Layout(article.Title) {
		<div class="container">
			<h1>{ article.Title }</h1>
			<p>{ article.Content }</p>
		</div>
	}
}
